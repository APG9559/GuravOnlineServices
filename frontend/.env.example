# When running with Docker Compose, nginx proxies /api to the backend
# and this variable is NOT needed.
#
# Only set this if you deploy frontend separately from the backend:
# VITE_API_URL=https://your-backend-domain.com

# UPI Dynamic QR Code configurations
VITE_UPI_ID_VAISHALI=
VITE_UPI_ID_ASHISH=
VITE_UPI_ID_PARSHURAM=
VITE_UPI_ID_GAURI=
